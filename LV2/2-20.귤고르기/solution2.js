// https://school.programmers.co.kr/learn/courses/30/lessons/138476

function solution(k, tangerine) {
  return tangerine.sort((a, b) => a - b);
  const hash = new Map();
  for (const t of tangerine) {
    hash;
  }
}

console.log(solution(6, [1, 3, 2, 5, 4, 5, 2, 3])); // 3
